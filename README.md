# 📘 Module 11: JavaScript Strings & Objects

A complete collection of my **Module 11 JavaScript practice**. This module focuses on **String methods**, **Object fundamentals**, **Nested Objects**, and **Looping through Objects**.

---

## 📚 Topics Covered

### 🔤 JavaScript Strings

- String basics
- String immutability
- String comparison
- Case conversion
  - `toLowerCase()`
  - `toUpperCase()`
- `trim()`
- `includes()`
- `startsWith()`
- `endsWith()`
- `slice()`
- `concat()`
- `split()`
- `join()`
- Reverse a string using:
  - `split() + reverse() + join()`
  - `for...of` loop

---

### 📦 JavaScript Objects

- Object literals
- Key-value pairs
- Access object properties
- Dot notation
- Bracket notation
- Update object properties
- Add new properties
- Delete properties
- Nested objects
- Nested arrays

---

### 🔑 Object Methods

- `Object.keys()`
- `Object.values()`
- `Object.entries()`

---

### 🔁 Looping Through Objects

- `for...of`
- `for...in`
- Looping through arrays
- Looping through object keys
- Accessing object values inside loops

---

## 🛠️ Skills Practiced

- Working with strings
- Manipulating text
- Comparing strings
- Reversing strings
- Creating objects
- Reading and updating object properties
- Using nested objects
- Accessing nested data
- Working with arrays inside objects
- Iterating over arrays and objects
- Using built-in JavaScript object methods

---

## 📂 Folder Structure

```text
module-11/
│
├── string.js
├── comparison.js
├── includes.js
├── slice.js
├── concat.js
├── split-join.js
├── reverse.js
├── object.js
├── object-access.js
├── bracket-notation.js
├── object-methods.js
├── nested-object.js
├── object-loop.js
└── README.md
```

> **Note:** File names may vary depending on your project structure.

---

## 🎯 Learning Outcomes

After completing this module, I can:

- ✅ Work confidently with JavaScript strings.
- ✅ Use common string methods for text manipulation.
- ✅ Reverse strings using different approaches.
- ✅ Create and manage JavaScript objects.
- ✅ Access and update object properties.
- ✅ Work with nested objects and arrays.
- ✅ Use `Object.keys()`, `Object.values()`, and `Object.entries()`.
- ✅ Loop through arrays and objects efficiently.
- ✅ Understand the difference between `for...of` and `for...in`.

---

## 🚀 Technologies Used

- JavaScript (ES6)
- Node.js
- Visual Studio Code
- Git & GitHub

---

## 📖 What I Learned

This module strengthened my understanding of how JavaScript handles **strings** and **objects**. I practiced using built-in methods, explored nested data structures, and learned how to iterate through arrays and objects. These concepts are fundamental for working with APIs, JSON data, and modern JavaScript frameworks.

---

## ▶️ How to Run

```bash
node filename.js
```

Example:

```bash
node reverse.js
```

---

## 📌 Git Commands

```bash
git add .
git commit -m "Complete Module 11 JavaScript Strings and Objects"
git push
```

---

## 👨‍💻 Author

**Arafat Islam**

Learning JavaScript step by step while building a strong foundation for modern web development. 🚀